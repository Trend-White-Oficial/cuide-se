import { ReactNode } from 'react';
type AdminLayoutProps = {
    children: ReactNode;
    title: string;
};
declare const AdminLayout: ({ children, title }: AdminLayoutProps) => import("react/jsx-runtime").JSX.Element;
export default AdminLayout;
